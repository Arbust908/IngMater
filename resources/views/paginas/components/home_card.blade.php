<a href="{{ url($card['slug']) }}" class="bg-main-200 px-6 py-4 text-white text-base mb-3 flex shadow-md items-center rounded-xxl">
    <img src="/img/{{ $card['img'] }}" alt="Icono de {{$card['title']}}" class="mr-6 font-bold text-sm">
    <h3>{{ $card['title'] }}</h3>
</a>
